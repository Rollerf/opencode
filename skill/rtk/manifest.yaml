name: "rtk"
display_name: "RTK"
description: "Optional CLI proxy and OpenCode hook guidance for compact shell command output and token savings."
default_prompt: "Use $rtk when RTK is installed for shell commands with large or noisy output; fall back to normal commands when unavailable."
compatibility:
  codex: true
  github_copilot: true
invocation:
  explicit_tag: "$rtk"
