# generic Pack

Fallback pack for projects without a dedicated stack pack.

## Usage

1. Fill `verification_commands` and `tdd_commands` placeholders.
2. Keep local-only operation and prohibited actions intact.
3. Add stack-specific pack once repeated usage is identified.
